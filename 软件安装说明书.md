# HealthyAssistant 健康助手安装说明书

## 环境要求

### 基础环境
- Java Development Kit (JDK) 17 或更高版本
- Node.js 16.0 或更高版本
- Maven 3.6 或更高版本
- MySQL 8.0 或更高版本

### 数据库配置
1. 创建 MySQL 数据库：
```sql
CREATE DATABASE healthyassistant CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

## 后端部署与启动步骤

1. 进入后端目录：
```bash
cd backend
```

2. 修改数据库配置：
编辑 `src/main/resources/application.properties` 文件，更新以下配置：
```properties
spring.datasource.url=jdbc:mysql://localhost:3306/healthyassistant
spring.datasource.username=你的数据库用户名
spring.datasource.password=你的数据库密码
```

3. 打包后端应用：
```bash
mvn clean package -DskipTests
```

4. 启动后端服务（推荐）：
```bash
cd backend
mvn spring-boot:run
```

- 启动成功后，命令行会显示 `Started BackEndApplication` 字样。
- 后端默认端口为 8000。
- 如遇 `No plugin found for prefix 'spring-boot'` 错误，请检查 `pom.xml` 文件中是否包含如下内容：

```xml
<plugin>
  <groupId>org.springframework.boot</groupId>
  <artifactId>spring-boot-maven-plugin</artifactId>
</plugin>
```

- 如果缺失，请补充到 `<build><plugins>` 节点下，并重新执行 `mvn clean package`。

5. 也可以使用如下命令启动（需已打包）：
```bash
java -jar target/backend-0.0.1-SNAPSHOT.jar
```

## 前端部署与启动步骤

1. 进入前端目录：
```bash
cd frontend
```

2. 安装依赖：
```bash
npm install
```

3. 构建前端应用：
```bash
npm run build
```

4. 启动前端服务（需全局安装 http-server）：

如未安装 http-server，请先执行：
```bash
npm install -g http-server
```

进入前端构建目录并启动服务：
```bash
cd dist
http-server -p 5173
```

- 启动成功后，命令行会显示 `Serving!` 字样。
- 前端默认端口为 5173。

如果启动失败，也可以使用如下命令启动（需已构建）：
```bash
    npm run dev
```

手动打开浏览器访问：
```
http://localhost:5173
```

## 运行顺序建议

1. 先启动 MySQL 数据库服务。
2. 启动后端服务（见上文）。
3. 启动前端服务（见上文）。
4. 浏览器访问前端页面。

## 常见问题解决

1. 端口占用：
   - 后端默认端口：8000
   - 前端默认端口：5173
   如端口被占用，可修改端口号。

2. 数据库连接失败：
   - 检查 MySQL 服务是否启动
   - 检查数据库用户名和密码是否正确
   - 检查数据库名称是否正确

3. 文件上传问题：
   - 确保 `backend/uploads` 目录存在且有写入权限

## 注意事项

1. 首次运行时需要：
   - 创建数据库
   - 配置数据库连接信息
   - 确保所有依赖都已正确安装

2. 安全建议：
   - 修改默认数据库密码
   - 在生产环境中配置 HTTPS
   - 定期备份数据库

3. 性能优化：
   - 根据服务器配置调整 JVM 参数
   - 配置合适的数据库连接池大小

## 技术支持

如遇到问题，请参考：
1. 项目 GitHub 仓库的 Issues 区
2. 查看应用日志文件
   - 后端日志：`backend/logs/`
   - 前端控制台日志

## 版本信息

- 当前版本：1.0.0
- 最后更新：2025-06-30
- 支持平台：Windows