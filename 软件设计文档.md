# HealthyAssistant 软件设计文档

## 一、系统整体架构

![系统架构图](./截图/系统架构图.png)

**详细说明：**
- **用户**：通过浏览器或移动端访问系统。
- **前端**：采用 Vue3 技术栈，包含页面组件、全局状态管理、路由跳转等。
- **API请求**：前端通过 REST API 与后端通信，所有数据交互均走 API 层。
- **后端**：Spring Boot 实现，分为控制器（Controller）、服务层（Service）、数据访问层（Repository）。
- **认证/鉴权**：所有敏感操作需登录，接口有权限校验。
- **AI服务**：后端通过 HTTP/SDK 调用外部AI（如智谱API）实现文生图、文生视频、智能推荐等功能。
- **文件存储**：支持本地或云端存储用户上传的图片、视频等文件。
- **数据库**：MySQL 存储所有结构化数据（用户、记录、分享、评论等）。

---

## 二、主要模块与功能分布

### 1. 前端（Vue3）
- **页面结构**：登录注册、个人主页、信息完善、运动计划、运动记录、社区、推荐、AI对话等。
- **全局组件**：NavBar、ChatBox、ImageUpload、VideoPlayer等。
- **状态管理**：Vuex，管理用户登录状态、用户信息、全局提示等。
- **路由管理**：Vue Router，支持页面跳转和权限控制。
- **UI风格**：健康、清新、响应式，支持移动端和PC端。

### 2. 后端（Spring Boot）
- **控制器（Controller）**：负责API路由和参数校验。
- **服务层（Service）**：封装业务逻辑，如用户、计划、推荐、AI生成等。
- **数据访问层（Repository）**：基于JPA操作数据库。
- **AI集成**：通过HTTP调用智谱等AI服务，支持文生图、文生视频、智能推荐。
- **安全机制**：登录校验、用户信息校验、接口权限控制。

### 3. 数据库（MySQL）
- **用户表**：存储用户基本信息、档案、偏好等。
- **运动记录表**：存储用户每日运动打卡、内容等。
- **社区分享表**：存储用户发布的运动分享、图片、评论、点赞等。
- **AI任务表**：存储AI生成图片、视频的任务和结果。

---

## 三、主要类图

![主要类图](./截图/实体类图.png)

**说明：**
- User为核心，关联档案、记录、分享、视频任务等。
- Share与Comment为社区核心内容。
- 采用一对多/多对一关系建模。

---

## 四、服务与控制器类图

![服务与控制器类图](./截图/服务和控制类图.png)

**说明：**
- 每个 Controller 负责对应模块的 API 路由与参数校验，调用 Service 实现具体业务。
- Service 层封装业务逻辑，可复用、便于测试和扩展。
- PlanService 依赖推荐、AI图片、AI视频等服务实现智能运动计划。
- 各 Controller/Service 职责分明，便于维护和扩展。

---

## 五、关键业务流程

### 1. 用户注册与登录
- 用户注册时填写基本信息，后端校验并存储。
- 登录后，前端自动检查用户信息完整性，未完善则跳转信息完善页。

### 2. 运动计划生成
- 用户完善信息后，前端请求后端生成个性化运动计划。
- 后端根据用户特征、兴趣等调用推荐服务，生成一周计划并返回。

### 3. 社区内容推荐
- 用户进入社区首页，后端根据用户兴趣、目标、历史行为等返回推荐内容。
- 推荐区与普通内容区风格一致，支持点赞、评论、浏览等交互。

### 4. AI文生图/视频
- 用户在运动计划或AI专区输入描述，前端发起生成请求。
- 后端调用AI服务生成图片/视频，返回URL，前端展示。

### 5. 运动记录与打卡
- 用户可手动添加、编辑、删除运动记录。
- 记录与用户、日期、类型等关联，便于后续统计与展示。

---

## 六、数据库设计（简要）

- user（用户表）：id, username, password, gender, age, height, weight, goal, interest, part, avatar
- user_profile（用户档案表）：id, user_id, bio, avatar
- user_record（运动记录表）：id, user_id, date, type, content
- share（社区分享表）：id, author_id, title, content, images, likes, views, created_at
- comment（评论表）：id, share_id, author_id, content, created_at
- video_task（AI视频任务表）：id, user_id, video_url, status

---

## 七、前后端交互接口（示例）

- `/api/auth/register`：用户注册
- `/api/auth/login`：用户登录
- `/api/auth/update`：更新用户信息
- `/api/plan/generate`：生成运动计划
- `/api/plan/get`：获取运动计划
- `/api/shares/get`：获取社区分享
- `/api/shares/post`：发布分享
- `/api/comment/add`：添加评论
- `/api/ai/image`：AI文生图
- `/api/ai/video`：AI文生视频
- `/api/recommendations`：获取个性化推荐

---

## 八、用到的设计模式

1. **单例模式（Singleton）**
   - Spring Boot 的 Service、Repository、Controller 默认单例。
   - 例如：`@Service`、`@Repository` 注解的类。

2. **工厂模式（Factory）**
   - Spring 容器本身就是工厂模式的实现，负责 Bean 的创建和管理。
   - 例如：自动注入的 Service/Repository。

3. **策略模式（Strategy）**
   - 推荐算法、AI生成等可通过策略模式扩展不同实现。
   - 例如：`RecommendationService` 可根据不同用户特征选择不同推荐策略。

---

## 九、系统安全与扩展性

- **安全**：所有敏感操作需登录，接口有权限校验，防止未授权访问。
- **扩展性**：AI服务、推荐算法、社区功能均可模块化扩展，便于后续升级。
- **多端兼容**：前端响应式设计，支持PC和移动端。

---

## 十、项目规模与页面统计

- **项目总代码量**：约 8,000 行（含前后端、样式、配置等，实际可扩展）
- **主要前端页面数量**：约 12 个（如登录注册、个人主页、信息完善、运动计划、运动记录、社区、推荐、AI对话等）
- **主要后端接口数量**：约 20 个（用户、计划、社区、AI、推荐等相关API）

> 以上数据为估算，实际项目可根据需求持续扩展。 